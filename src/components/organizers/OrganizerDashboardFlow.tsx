/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import Link from "next/link";import {type FormEvent,useEffect,useMemo,useState} from "react";import styles from "./OrganizerDashboardFlow.module.css";
const SUPABASE_URL="https://qrouwtqsqrfeeeppyeru.supabase.co",SUPABASE_PUBLISHABLE_KEY="sb_publishable_vR9wivNa_fIb0QKmqua6Wg_H_7OPvUk",SESSION_KEY="noxa-organizer-session-v1";const EVENT_TYPES=[["car_meet","Car meet"],["cars_and_coffee","Cars & Coffee"],["group_drive","Group drive"],["moto_meet","Moto meet"],["show","Auto show"],["festival","Festival"],["track_day","Track day"],["drag","Drag racing"],["drift","Drift"],["rally","Rally"],["other","Other"]] as const;type Locale="en"|"el";type Phase="checking"|"signed_out"|"ready";type EventStatus="published"|"unpublished"|"cancelled";type OrganizerSession={accessToken:string;refreshToken:string;expiresAt:number;email:string;userId:string};type Membership={organizer_id:string;role:"owner"|"admin"|"editor";status:string};type Organizer={id:string;slug:string;name:string;organizer_type:"community"|"team"|"company"|"page"|"group";community_id:string|null;city:string|null;country_code:string;instagram_url:string|null;website_url:string|null;verified:boolean;status:string};type ManagedEvent={id:string;organizer_profile_id:string;title:string;event_type:string;starts_at:string;ends_at:string|null;timezone:string;location_text:string|null;city:string|null;region:string|null;summary:string|null;status:EventStatus;source_url:string;updated_at:string};type DashboardData={memberships:Membership[];organizers:Organizer[];events:ManagedEvent[]};type AuthUser={id:string;email?:string};type EventDraft={title:string;eventType:string;startsAt:string;endsAt:string;city:string;region:string;location:string;summary:string};
const copy={en:{heading:"Organizer Dashboard",signOut:"Sign out",orgs:"My organizations",verified:"Verified",pending:"Pending verification",create:"Create event",edit:"Edit event",myEvents:"My events",noEvents:"No events yet.",noAccess:"No organizer access yet.",noAccessBody:"Organizer access is granted only after NOXA approves an Organizer application or sends you an invite.",apply:"Apply as Organizer",browse:"Browse Organizers",email:"Email",signIn:"Organizer access",send:"Send sign-in link",sent:"Link sent.",back:"Back to NOXA",event:"Event",place:"Place",review:"Review",eventName:"Event name",type:"Type",starts:"Starts",ends:"Ends",city:"City",region:"Region",location:"Location",description:"Description",optional:"optional",next:"Continue",previous:"Back",saveDraft:"Save draft",publish:"Publish event",saveChanges:"Save changes",editAction:"Edit",unpublish:"Unpublish",publishAction:"Publish",cancel:"Cancel event",locked:"Verification required."},el:{heading:"Organizer Dashboard",signOut:"Αποσύνδεση",orgs:"Οι οργανισμοί μου",verified:"Verified",pending:"Αναμονή verification",create:"Δημιουργία event",edit:"Επεξεργασία event",myEvents:"Τα events μου",noEvents:"Δεν υπάρχουν events ακόμη.",noAccess:"Δεν έχεις organizer access ακόμη.",noAccessBody:"Το organizer access δίνεται μόνο αφού το NOXA εγκρίνει αίτηση Organizer ή σου στείλει invite.",apply:"Αίτηση ως Organizer",browse:"Δες Organizers",email:"Email",signIn:"Organizer access",send:"Στείλε link σύνδεσης",sent:"Το link στάλθηκε.",back:"Πίσω στο NOXA",event:"Event",place:"Τοποθεσία",review:"Έλεγχος",eventName:"Όνομα event",type:"Τύπος",starts:"Έναρξη",ends:"Λήξη",city:"Πόλη",region:"Περιοχή",location:"Τοποθεσία",description:"Περιγραφή",optional:"προαιρετικό",next:"Συνέχεια",previous:"Πίσω",saveDraft:"Αποθήκευση draft",publish:"Δημοσίευση event",saveChanges:"Αποθήκευση",editAction:"Edit",unpublish:"Unpublish",publishAction:"Publish",cancel:"Cancel event",locked:"Απαιτείται verification."}} as const;
function headers(token?:string){return{apikey:SUPABASE_PUBLISHABLE_KEY,"Content-Type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{})}}function readSession():OrganizerSession|null{try{const raw=window.localStorage.getItem(SESSION_KEY);return raw?JSON.parse(raw) as OrganizerSession:null}catch{return null}}function storeSession(s:OrganizerSession|null){if(s)window.localStorage.setItem(SESSION_KEY,JSON.stringify(s));else window.localStorage.removeItem(SESSION_KEY)}function parseMagicLinkSession():Omit<OrganizerSession,"email"|"userId">|null{if(!window.location.hash)return null;const p=new URLSearchParams(window.location.hash.slice(1)),accessToken=p.get("access_token"),refreshToken=p.get("refresh_token"),expiresIn=Number(p.get("expires_in")??"3600");if(!accessToken||!refreshToken)return null;window.history.replaceState({},document.title,window.location.pathname+window.location.search);return{accessToken,refreshToken,expiresAt:Date.now()+Math.max(60,expiresIn)*1000}}async function getUser(token:string):Promise<AuthUser|null>{const r=await fetch(`${SUPABASE_URL}/auth/v1/user`,{headers:headers(token),cache:"no-store"});return r.ok?await r.json() as AuthUser:null}async function refreshSession(refreshToken:string):Promise<OrganizerSession|null>{const r=await fetch(`${SUPABASE_URL}/auth/v1/token?grant_type=refresh_token`,{method:"POST",headers:headers(),body:JSON.stringify({refresh_token:refreshToken})});if(!r.ok)return null;const p=await r.json() as {access_token:string;refresh_token:string;expires_in:number;user:AuthUser};if(!p.user.email)return null;return{accessToken:p.access_token,refreshToken:p.refresh_token,expiresAt:Date.now()+(p.expires_in||3600)*1000,email:p.user.email,userId:p.user.id}}async function claimInvites(token:string){await fetch(`${SUPABASE_URL}/rest/v1/rpc/claim_organizer_invites`,{method:"POST",headers:headers(token),body:"{}",cache:"no-store"})}async function loadDashboard(s:OrganizerSession):Promise<DashboardData>{await claimInvites(s.accessToken);const mr=await fetch(`${SUPABASE_URL}/rest/v1/organizer_admins?select=organizer_id,role,status&user_id=eq.${encodeURIComponent(s.userId)}&status=eq.active`,{headers:headers(s.accessToken),cache:"no-store"});if(!mr.ok)throw new Error("Unable to load organizer access.");const memberships=await mr.json() as Membership[];if(!memberships.length)return{memberships,organizers:[],events:[]};const ids=memberships.map(x=>x.organizer_id),f=`(${ids.join(",")})`,[or,er]=await Promise.all([fetch(`${SUPABASE_URL}/rest/v1/organizer_profiles?select=id,slug,name,organizer_type,community_id,city,country_code,instagram_url,website_url,verified,status&id=in.${f}&order=name.asc`,{headers:headers(s.accessToken),cache:"no-store"}),fetch(`${SUPABASE_URL}/rest/v1/radar_events?select=id,organizer_profile_id,title,event_type,starts_at,ends_at,timezone,location_text,city,region,summary,status,source_url,updated_at&organizer_profile_id=in.${f}&publication_source=eq.organizer&order=starts_at.desc&limit=300`,{headers:headers(s.accessToken),cache:"no-store"})]);if(!or.ok||!er.ok)throw new Error("Unable to load organizer dashboard.");return{memberships,organizers:await or.json() as Organizer[],events:await er.json() as ManagedEvent[]}}function sourceForOrganizer(o:Organizer){return o.instagram_url||o.website_url||(o.community_id?`https://noxastreetapp.com/communities/${o.slug}`:"")}function localInputValue(v:string|null){if(!v)return"";const d=new Date(v);if(Number.isNaN(d.getTime()))return"";return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16)}function formatDate(v:string,l:Locale){const d=new Date(v);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat(l==="el"?"el-GR":"en-GB",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"}).format(d)}function eventTypeLabel(v:string){return EVENT_TYPES.find(([t])=>t===v)?.[1]??"Event"}
function EventWizard({locale,organizer,editing,busy,onSave,onCancelEdit}:{locale:Locale;organizer:Organizer;editing:ManagedEvent|null;busy:boolean;onSave:(d:EventDraft,s:EventStatus)=>Promise<boolean>;onCancelEdit:()=>void}){const t=copy[locale];const[step,setStep]=useState(1),[error,setError]=useState(""),[draft,setDraft]=useState<EventDraft>({title:editing?.title??"",eventType:editing?.event_type??"car_meet",startsAt:localInputValue(editing?.starts_at??null),endsAt:localInputValue(editing?.ends_at??null),city:editing?.city??organizer.city??"",region:editing?.region??"",location:editing?.location_text??"",summary:editing?.summary??""});function update<K extends keyof EventDraft>(k:K,v:EventDraft[K]){setDraft(c=>({...c,[k]:v}));setError("")}function goNext(){if(step===1&&!draft.title.trim()){setError(locale==="el"?"Βάλε όνομα event.":"Add an event name.");return}if(step===2){if(!draft.startsAt||!draft.city.trim()||!draft.location.trim()){setError(locale==="el"?"Συμπλήρωσε ημερομηνία, πόλη και τοποθεσία.":"Add date, city and location.");return}const s=new Date(draft.startsAt),e=draft.endsAt?new Date(draft.endsAt):null;if(Number.isNaN(s.getTime())||(e&&Number.isNaN(e.getTime()))){setError(locale==="el"?"Έλεγξε την ημερομηνία.":"Check the date and time.");return}if(e&&e<=s){setError(locale==="el"?"Η λήξη πρέπει να είναι μετά την έναρξη.":"End time must be after start time.");return}}setStep(c=>Math.min(3,c+1));setError("")}async function submit(status:EventStatus){setError("");const ok=await onSave(draft,status);if(ok&&!editing){setStep(1);setDraft({title:"",eventType:"car_meet",startsAt:"",endsAt:"",city:organizer.city??"",region:"",location:"",summary:""})}}return <div className={styles.wizard}><div className={styles.progress} aria-label="Event creation progress">{[t.event,t.place,t.review].map((label,index)=>{const n=index+1;return <span className={step===n?styles.progressActive:step>n?styles.progressDone:styles.progressIdle} key={label}>{n}<small>{label}</small></span>})}</div>{step===1?<div className={styles.stepPanel}><div className={styles.organizerLine}><span>Organizer</span><strong>{organizer.name}</strong><small>✓ {t.verified}</small></div><label><span>{t.eventName}</span><input autoFocus maxLength={160} onChange={e=>update("title",e.target.value)} value={draft.title}/></label><label><span>{t.type}</span><select onChange={e=>update("eventType",e.target.value)} value={draft.eventType}>{EVENT_TYPES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label></div>:null}{step===2?<div className={styles.stepPanel}><div className={styles.twoColumns}><label><span>{t.starts}</span><input onChange={e=>update("startsAt",e.target.value)} type="datetime-local" value={draft.startsAt}/></label><label><span>{t.ends} <em>{t.optional}</em></span><input onChange={e=>update("endsAt",e.target.value)} type="datetime-local" value={draft.endsAt}/></label></div><div className={styles.twoColumns}><label><span>{t.city}</span><input maxLength={100} onChange={e=>update("city",e.target.value)} value={draft.city}/></label><label><span>{t.region} <em>{t.optional}</em></span><input maxLength={100} onChange={e=>update("region",e.target.value)} value={draft.region}/></label></div><label><span>{t.location}</span><input maxLength={180} onChange={e=>update("location",e.target.value)} value={draft.location}/></label></div>:null}{step===3?<div className={styles.stepPanel}><article className={styles.previewCard}><span>{eventTypeLabel(draft.eventType).toUpperCase()}</span><h3>{draft.title||"Event"}</h3><p>{draft.startsAt?formatDate(new Date(draft.startsAt).toISOString(),locale):"—"}</p><p>{[draft.city,draft.location].filter(Boolean).join(" · ")||"—"}</p><small>{organizer.name} ✓</small></article><label><span>{t.description} <em>{t.optional}</em></span><textarea maxLength={1200} onChange={e=>update("summary",e.target.value)} rows={4} value={draft.summary}/></label></div>:null}{error?<p className={styles.inlineError} role="alert">{error}</p>:null}<div className={styles.wizardActions}>{step>1?<button className={styles.secondaryButton} disabled={busy} onClick={()=>{setStep(c=>c-1);setError("")}} type="button">← {t.previous}</button>:editing?<button className={styles.secondaryButton} onClick={onCancelEdit} type="button">{t.previous}</button>:<span/>}{step<3?<button className={styles.primaryButton} onClick={goNext} type="button">{t.next} →</button>:editing?<button className={styles.primaryButton} disabled={busy} onClick={()=>void submit(editing.status)} type="button">{busy?"…":t.saveChanges}</button>:<div className={styles.finalActions}><button className={styles.secondaryButton} disabled={busy} onClick={()=>void submit("unpublished")} type="button">{t.saveDraft}</button><button className={styles.primaryButton} disabled={busy} onClick={()=>void submit("published")} type="button">{busy?"…":t.publish}</button></div>}</div></div>}
export function OrganizerDashboardFlow({locale}:{locale:Locale}){const t=copy[locale];const[phase,setPhase]=useState<Phase>("checking"),[session,setSession]=useState<OrganizerSession|null>(null),[data,setData]=useState<DashboardData>({memberships:[],organizers:[],events:[]}),[email,setEmail]=useState(""),[busy,setBusy]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState(""),[editing,setEditing]=useState<ManagedEvent|null>(null),[selectedOrganizerId,setSelectedOrganizerId]=useState("");const activeOrganizer=useMemo(()=>data.organizers.find(x=>x.id===selectedOrganizerId)??data.organizers[0],[data.organizers,selectedOrganizerId]);async function hydrate(s:OrganizerSession){const d=await loadDashboard(s);storeSession(s);setSession(s);setData(d);setSelectedOrganizerId(c=>c||d.organizers[0]?.id||"");setPhase("ready")}useEffect(()=>{let cancelled=false;void(async()=>{try{const magic=parseMagicLinkSession();if(magic){const user=await getUser(magic.accessToken);if(!user?.email||cancelled)return setPhase("signed_out");if(!cancelled)await hydrate({...magic,email:user.email,userId:user.id});return}let s=readSession();if(!s)return setPhase("signed_out");if(s.expiresAt<=Date.now()+60000)s=await refreshSession(s.refreshToken);if(!s){storeSession(null);return setPhase("signed_out")}if(!cancelled)await hydrate(s)}catch(cause){storeSession(null);if(!cancelled){setError(cause instanceof Error?cause.message:"Unable to restore organizer access.");setPhase("signed_out")}}})();return()=>{cancelled=true}},[]);async function requestMagicLink(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");setMessage("");try{const route=locale==="el"?"/el/organizer":"/organizer",redirectTo=`${window.location.origin}${route}`,r=await fetch(`${SUPABASE_URL}/auth/v1/otp?redirect_to=${encodeURIComponent(redirectTo)}`,{method:"POST",headers:headers(),body:JSON.stringify({email:email.trim(),create_user:true})});if(!r.ok)throw new Error("Unable to send sign-in link.");setMessage(t.sent)}catch(cause){setError(cause instanceof Error?cause.message:"Unable to send sign-in link.")}finally{setBusy(false)}}async function refreshData(){if(session)setData(await loadDashboard(session))}async function saveEvent(draft:EventDraft,status:EventStatus){if(!session||!activeOrganizer||busy)return false;setBusy(true);setError("");setMessage("");try{if(!activeOrganizer.verified||activeOrganizer.status!=="active")throw new Error(t.locked);const sourceUrl=sourceForOrganizer(activeOrganizer);if(!sourceUrl)throw new Error("Organizer needs Instagram or website.");const starts=new Date(draft.startsAt),ends=draft.endsAt?new Date(draft.endsAt):null;if(Number.isNaN(starts.getTime()))throw new Error("Invalid start time.");if(ends&&(Number.isNaN(ends.getTime())||ends<=starts))throw new Error("Invalid end time.");const payload={title:draft.title.trim(),event_type:draft.eventType,starts_at:starts.toISOString(),ends_at:ends?.toISOString()??null,timezone:activeOrganizer.country_code==="GR"?"Europe/Athens":"UTC",location_text:draft.location.trim(),city:draft.city.trim(),region:draft.region.trim()||null,summary:draft.summary.trim()||null,status,updated_at:new Date().toISOString()};if(editing){const r=await fetch(`${SUPABASE_URL}/rest/v1/radar_events?id=eq.${encodeURIComponent(editing.id)}`,{method:"PATCH",headers:{...headers(session.accessToken),Prefer:"return=minimal"},body:JSON.stringify(payload)});if(!r.ok)throw new Error("Could not update event.");setMessage("Event updated.");setEditing(null)}else{const r=await fetch(`${SUPABASE_URL}/rest/v1/radar_events`,{method:"POST",headers:{...headers(session.accessToken),Prefer:"return=minimal"},body:JSON.stringify({...payload,organizer_profile_id:activeOrganizer.id,publication_source:"organizer",country_code:activeOrganizer.country_code,organizer_name:activeOrganizer.name,organizer_url:sourceUrl,source_name:activeOrganizer.name,source_url:sourceUrl,candidate_id:null,source_id:null})});if(!r.ok)throw new Error("Could not create event.");setMessage(status==="published"?"Event published.":"Draft saved.")}await refreshData();return true}catch(cause){setError(cause instanceof Error?cause.message:"Could not save event.");return false}finally{setBusy(false)}}async function setStatus(id:string,status:EventStatus){if(!session||busy)return;setBusy(true);setError("");try{const r=await fetch(`${SUPABASE_URL}/rest/v1/radar_events?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{...headers(session.accessToken),Prefer:"return=minimal"},body:JSON.stringify({status,updated_at:new Date().toISOString()})});if(!r.ok)throw new Error("Could not update event status.");await refreshData()}catch(cause){setError(cause instanceof Error?cause.message:"Could not update event status.")}finally{setBusy(false)}}function signOut(){storeSession(null);setSession(null);setData({memberships:[],organizers:[],events:[]});setPhase("signed_out")}if(phase==="checking")return <main className={styles.state}>Checking access…</main>;if(phase==="signed_out")return <main className={styles.signInPage}><Link className={styles.brand} href={locale==="el"?"/el":"/"}>NOXA</Link><section className={styles.signInCard}><p className={styles.eyebrow}>NOXA ORGANIZER</p><h1>{t.signIn}</h1><form onSubmit={requestMagicLink}><label><span>{t.email}</span><input autoComplete="email" onChange={e=>setEmail(e.target.value)} required type="email" value={email}/></label><button disabled={busy} type="submit">{busy?"…":t.send}</button></form>{message?<p className={styles.success}>{message}</p>:null}{error?<p className={styles.error} role="alert">{error}</p>:null}<Link className={styles.backLink} href={locale==="el"?"/el":"/"}>← {t.back}</Link></section></main>;if(!data.organizers.length){const base=locale==="el"?"/el":"";return <main className={styles.state}><p className={styles.eyebrow}>NOXA ORGANIZER</p><h1>{t.noAccess}</h1><p>{t.noAccessBody}</p><Link className={styles.primaryLink} href={`${base}/organizers/apply`}>{t.apply}</Link><Link className={styles.backLink} href={`${base}/organizers`}>{t.browse}</Link></main>}return <div className={styles.page}><header className={styles.header}><Link className={styles.brand} href={locale==="el"?"/el":"/"}>NOXA</Link><button className={styles.signOut} onClick={signOut} type="button">{t.signOut}</button></header><main className={styles.main}><section className={styles.heading}><p className={styles.eyebrow}>NOXA ORGANIZER</p><h1>{t.heading}</h1></section>{message?<p className={styles.success}>{message}</p>:null}{error?<p className={styles.error} role="alert">{error}</p>:null}<section className={styles.organizations}><div className={styles.sectionTitle}><h2>{t.orgs}</h2><span>{data.organizers.length}</span></div><div className={styles.orgGrid}>{data.organizers.map(o=>{const m=data.memberships.find(x=>x.organizer_id===o.id),active=activeOrganizer?.id===o.id;return <button className={active?styles.orgCardActive:styles.orgCard} key={o.id} onClick={()=>{setSelectedOrganizerId(o.id);setEditing(null)}} type="button"><span>{o.organizer_type} · {m?.role}</span><strong>{o.name}</strong><small>{o.verified?t.verified:t.pending}</small></button>})}</div></section><div className={styles.workspace}><section className={styles.editor}><div className={styles.sectionTitle}><h2>{editing?t.edit:t.create}</h2>{editing?<button className={styles.textButton} onClick={()=>setEditing(null)} type="button">×</button>:null}</div>{!activeOrganizer?.verified?<div className={styles.locked}>{t.locked}</div>:<EventWizard busy={busy} editing={editing} key={`${activeOrganizer.id}-${editing?.id??"new"}`} locale={locale} onCancelEdit={()=>setEditing(null)} onSave={saveEvent} organizer={activeOrganizer}/>}</section><section className={styles.eventsSection}><div className={styles.sectionTitle}><h2>{t.myEvents}</h2><span>{data.events.length}</span></div>{data.events.length?<div className={styles.eventList}>{data.events.map(item=>{const o=data.organizers.find(org=>org.id===item.organizer_profile_id);return <article className={styles.eventCard} key={item.id}><div className={styles.eventTop}><div><span>{o?.name??"Organizer"}</span><h3>{item.title}</h3></div><span className={styles.status}>{item.status}</span></div><p>{formatDate(item.starts_at,locale)} · {item.city??item.location_text??"—"}</p><div className={styles.eventActions}><button onClick={()=>{setSelectedOrganizerId(item.organizer_profile_id);setEditing(item)}} type="button">{t.editAction}</button>{item.status==="published"?<button onClick={()=>void setStatus(item.id,"unpublished")} type="button">{t.unpublish}</button>:null}{item.status==="unpublished"?<button onClick={()=>void setStatus(item.id,"published")} type="button">{t.publishAction}</button>:null}{item.status!=="cancelled"?<button className={styles.danger} onClick={()=>void setStatus(item.id,"cancelled")} type="button">{t.cancel}</button>:null}</div></article>})}</div>:<div className={styles.empty}>{t.noEvents}</div>}</section></div></main></div>}
